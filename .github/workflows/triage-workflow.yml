name: Triage Workflow Failures

on:
  workflow_run:
    workflows: ['**']
    types: [completed]

permissions:
  actions: write # required to re-run jobs

jobs:
  triage:
    uses: ./.github/workflows/reuse-triage-failure.yml
    with:
      workflow-run: "${{ toJson(github.event.workflow_run) }}"
